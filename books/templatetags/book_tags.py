from django import template

# register = template.Library()

# @register.filter
# def get_item(dictionary, key):
#     """Gets an item out of a dictionary given the key"""
#     return dictionary.get(key)